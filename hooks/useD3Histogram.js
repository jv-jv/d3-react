import { useEffect, useRef } from "react"

export default function useD3Histogram() {
  const divRef = useRef()

  useEffect(() => {
    // 1. Access Data

    // 2. Create Dimensions

    // 3. Draw Canvas

    // 4. Create Scales

    // 5. Draw Data

    // 6. Draw Peripherals

    // 7. Set Up Interactions

    // clean up
    return () => {}
  })

  return divRef
}
